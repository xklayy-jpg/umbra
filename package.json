{
  "name": "umbra-site",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "npx serve .",
    "build": "echo \"Static site - no build step\"",
    "lint": "eslint . --ext .js",
    "format": "prettier . --write"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "eslint-config-prettier": "^9.0.0",
    "prettier": "^3.2.0"
  }
}

